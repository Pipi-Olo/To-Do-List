spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/item
    driver-class-name: org.h2.Driver
    username: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  profiles:
    active: local